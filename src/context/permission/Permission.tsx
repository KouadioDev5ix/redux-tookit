import type { JSX } from "react";

type ProtectedRouteProps = {
  permission: string;
  children: React.ReactNode;
};

export const ProtectedRoutes = ({ permission, children }: ProtectedRouteProps) => {

 


};

